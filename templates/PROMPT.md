# Project instructions

You are working inside this repository. Describe the project here: what it is, the
language and tooling, conventions the generator must follow, and anything it should
never touch.

## Conventions

- Keep changes minimal and focused on the current task.
- Run the project's tests (via `run_command`) before declaring a task complete.
- Do not modify files unrelated to the task.

## Off limits

- (list files or directories the generator must not edit)
